<!DOCTYPE html>
<html lang="js">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet"  type='text/css' href="/css/style.css">
    <title>SignUp</title>
</head>
<body>
  <header>
      <%- include('header.ejs') %>
  </header>

    <% if (errors.length > 0) { %>
        <ul class="errors">
          <% errors.forEach(error =>{ %>
          <li> <%= error %> </li> 
          <% }); %>
        </ul>
      <% } %>
      <dev class="signup-box">
        <form action="/signup"  method="post" class="signup-form">
          <h1 class="signup-title">新規登録</h1>
          <p>ユーザー名</p>
          <dev class="signup-box">
            <input type="text" name="userName" class="signup-input">
          </dev>
          <p>メールアドレス</p>
          <dev class="signup-box">
            <input type="text" name="email" class="signup-input">
          </dev>
          <p>パスワード</p>
          <dev class="signup-box">
            <input type='password' name="password" class="signup-input">
          </dev>
          <p>確認用パスワード</p>
          <dev class="signup-box">
            <input type='password' name="password2" class="signup-input">
          </dev>
          <dev class="signup-submit">
            <input type="submit" value='登録'>
          </dev>
        </form>
     </dev>
     <dev class="jumpList">
      <a href="/">トップへ戻る</a>
    </dev>
</body>
</html>