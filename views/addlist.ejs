<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet"  type='text/css' href="/css/style.css">
    <title>Add List</title>
</head>
<body>
    <header>
        <%- include('header.ejs') %>
   </header>

    <% if(locals.isLoggedIn === false){ %> 
        <h1 class="need-login">ログインまたは新規登録してください</h1>
        <dev class="jumpList">
            <a href="/">トップへ戻る</a>
        </dev>
        <% } else {%>
            <dev class='form-box'>
                <form action="/create" method="post" class="add-form">
                    <h1 class="add-title">リストの追加</h1>
                    <p class="addSentence1">利用者氏名</p>
                    <dev class="add-box">
                        <input type="text" class="add-input" name="addListPaName">
                    </dev>
                    <p class="addSentence2">Todo</p>
                    <dev class="add-box">
                        <input type="text" class="add-input" name ='addListAction'>
                    </dev>
                    <dev class="add-submit">
                        <input type="submit" value="登録" class="addListSubmit">
                    </dev>
                </form>
                <dev class='jumpList'>
                    <a href="/list" class="btn">リストへ戻る</a>
                </dev>
            </dev>
     <% } %>
</body>
</html>